#
# Cloudflare WORKERS deploy config (matches the Cloudflare UI you showed):
# - Build command: npm run build
# - Deploy command: npx wrangler deploy
# - Path: /
#
# This Worker serves the Vite build output from ./dist as static assets, with SPA fallback to /index.html.
#

name = "joyfulcaller"
main = "src/worker.ts"
compatibility_date = "2024-12-15"

# Static assets served by the Worker (generated by `npm run build`)
assets = { directory = "./dist" }

